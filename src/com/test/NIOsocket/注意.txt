这个服务端的代码只使用了一个线程来处理所有的请求，但是依然可以同时连接多个客户端。selector的select方法会被阻塞，一旦注册的通道上有可用的事件，就会返回，继续执行下面的代码。

总的说来，上面的几段代码基本上只可以作为Socket和NIO的如何使用的代码，不适用于实际场景，比如说这里服务端并没有对连接进行管理，消息都是由客户端发送到服务器后响应的，服务端没有向指定客户端发送消息。但是，上面确实是长连接的实现，只是没有说明怎么使用长连接，其实这个才是关键的问题。下一篇会讨论基于框架（Mina和Netty）的长连接。